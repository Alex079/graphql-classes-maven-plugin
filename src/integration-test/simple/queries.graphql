
query itemsInDeliveryByPayment($paidBy: PaymentType!) {
	itemsInDelivery(paidBy: $paidBy) {
		...ItemFragment
	}
}

query trackingInfo($itemId: ID!) {
	trackingInfo(itemId: $itemId) {
		fromDate
		toDate
		toAddress
		delivered
	}
}