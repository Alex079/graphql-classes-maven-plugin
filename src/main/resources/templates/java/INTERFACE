package ${typesPackage};

<#list importSet as i>
import ${i}.*;
</#list>

public interface ${name}
{

<#list fields as f>
	${f.type.full} get${f.name?cap_first}();

	void set${f.name?cap_first}(${f.type.full} v);

</#list>
}
