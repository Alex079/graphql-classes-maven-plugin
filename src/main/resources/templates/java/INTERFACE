//CHECKSTYLE:OFF
package ${typesPackage};

<#list importPackages as i>
import ${i}.*;
</#list>

@SuppressWarnings({"all", "PMD"})
public interface ${name}
{

<#list fields as f>
	<#if useChainedAccessors>${name}<#else>void</#if> set${f.name?cap_first}(${f.type.full} v);

</#list>
}
//CHECKSTYLE:ON
